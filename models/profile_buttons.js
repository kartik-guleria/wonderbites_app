class ProfileButtonsCategory {
  constructor(id, image, options, nextScreen) {
    this.id = id;
    this.image = image;
    this.options = options;
    this.nextScreen = nextScreen;
  }
}
export default ProfileButtonsCategory;
