class WonderPoints {
  constructor(
    id,
    title,
    detailDesc,
    date,
    time,
    pointCount,
    productCount,
    image,
  ) {
    this.id = id;
    this.title = title;
    this.detailDesc = detailDesc;
    this.date = date;
    this.time = time;
    this.pointCount = pointCount;
    this.productCount = productCount;
    this.image = image;
  }
}
export default WonderPoints;
