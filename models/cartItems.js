class CartCategory {
  constructor(id, food, classification, totalPrice) {
    this.id = id;
    this.food = food;
    this.classification = classification;
    this.totalPrice = totalPrice;
  }
}
export default CartCategory;
